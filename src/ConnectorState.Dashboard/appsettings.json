{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "CrawlState": {
    "Server": "SQLPROD01",
    "Database": "ConnectorState",
    "Encrypt": true,
    "TrustServerCertificate": false,
    "ConnectTimeoutSeconds": 15,
    "CommandTimeoutSeconds": 60,
    "DefaultPageSize": 50,
    "SummaryWindowHours": 24,
    "ApplicationName": "ConnectorState.Dashboard",

    "//ReaderGroups": [
      "Groups whose members may read crawl state, as DOMAIN\\Group or a bare",
      "group name. Empty or absent means any authenticated user, which is what",
      "this site did before the setting existed. Membership is read from the",
      "Windows token, so a user added to a group must sign in again."
    ],
    "ReaderGroups": []
  }
}
