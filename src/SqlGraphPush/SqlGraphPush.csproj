<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>$(ConnectorTargetFramework)</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>SqlGraphPush</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!--
      The engine, and through it Connector.Security and the Graph
      SDK. Package versions and their advisory pins live in PushCore so
      there is one place to change them and the offline restore graph
      cannot drift between the push tools.
    -->
    <ProjectReference Include="..\PushCore\PushCore.csproj" />
    <ProjectReference Include="..\PushCore.State\PushCore.State.csproj" />
    <ProjectReference Include="..\PushCore.Sql\PushCore.Sql.csproj" />
  </ItemGroup>

  <ItemGroup>
    <None Update="appsettings.json">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup>

  <Import Project="$(MSBuildThisFileDirectory)..\..\build\SecretHygiene.targets" />

</Project>
